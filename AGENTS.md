# 画框 · FrameStudio

> 一个给照片添加拍摄参数相框的 HarmonyOS 应用。

---

## 一、设计目的

摄影爱好者在分享照片时，常常希望把光圈、快门、ISO 等 EXIF 信息优雅地嵌入画面边缘，形成「相框水印」效果。市面上的同类工具要么过于复杂，要么平台受限。本项目的目标是：

- **零学习成本**：选一个相框、挑一张照片、点保存，三步完成。
- **高度可定制**：边框颜色/厚度、文字颜色/大小、显示哪些参数、上下位置、对齐方式均可调。
- **本地优先**：所有照片处理在设备本地完成，不依赖云端。
- **沉浸体验**：全屏内容 + 悬浮胶囊导航，参考 iOS 原生图库的「底部胶囊」风格。

---

## 二、设计思路

### 2.1 整体架构

采用 HarmonyOS Stage 模式，单 EntryAbility，三页 Tab 结构：

| Tab | 名称 | 职责 |
|-----|------|------|
| 0 | 相册 | 管理相册、导入照片、网格浏览、进入编辑 |
| 1 | 设计 | 实时预览相框、选择预设、自定义参数、保存设计 |
| 2 | 设置 | 账号（演示）、会员购买（演示）、关于 |

导航方式为 **底部悬浮胶囊**（白色磨砂玻璃胶囊，~62% 宽，圆角 33），替代传统底部 TabBar，让内容可以滚动到导航下方，营造沉浸感。

页面路由使用 `Navigation` + `NavPathStack`，从相册进入相框编辑页时推栈，返回时弹栈。

### 2.2 视觉风格

- **底色**：相册/设计页 `#F2F2F2`，编辑页 `#111111`（深色沉浸）。
- **卡片**：白色圆角卡片，圆角 14，轻微阴影。
- **弹窗**：磨砂玻璃（`backdropBlur(32)` + 半透明背景 + 弥散阴影），参考 iOS 系统弹窗。
- **状态栏**：透明，图标颜色随页面切换（深色页面白色图标，浅色页面深色图标）。
- **图标**：统一使用 HarmonyOS Symbol 系统图标。

### 2.3 数据模型

#### 相框配置 `FrameConfig`
```
id / name / bg / textColor / borderRatio / fontSize / align / pos
showAperture / showShutter / showIso / showFocal / showCamera / showTime / showGps / showTitle / title
```
- 全部为原始类型字段，配合 `@Observed` / `@ObjectLink` 实现深度响应。
- 预设 5 套内置相框（极简白、经典黑、杂志、胶片、暗夜）。
- 用户自定义设计通过 `preferences` 持久化。

#### 相册 `AlbumStore`
```
Album { id, name, ts, photos[] }
PhotoRec { uri, ts, processed }
```
- 相册按创建时间倒序排列。
- 照片按导入顺序排列。
- 同样使用 `preferences` 持久化。

#### EXIF 信息 `ExifHelper`
- 通过 `image.ImageSource.getImageProperty` 读取原始 EXIF。
- 格式化：光圈 `f/1.8`、快门 `1/125s`、ISO `ISO100`、焦距 `24mm`、日期 `2024-05-01 12:34`、GPS `31°13′N 121°28′E`。

### 2.4 渲染与导出

- **预览**：`FramePreview` 组件与导出算法同源，读取照片尺寸后按比例缩放，使用 ArkUI 布局渲染，所见即所得。
- **导出**：`FrameExporter` 使用 `@kit.ArkGraphics2D` 的 `drawing.Canvas` 在离屏 `PixelMap` 上绘制：
  1. 填充相框底色
  2. 绘制照片（等比缩放，最大边 4096px 防 OOM）
  3. 使用 `TextBlob` 绘制文字行
  4. JPEG 打包（质量 94）→ 沙箱文件 → 调用系统相册保存对话框

### 2.5 交互细节

| 交互 | 说明 |
|------|------|
| 双指捏合 | 相册网格 1~5 列动态调整 |
| 长按照片 | 弹出确认，从相册移除 |
| 长按空相册 | 弹出确认，删除整个相册 |
| 相框 Chip 长按 | 删除自定义设计 |
| 编辑页返回 | 左上圆形按钮（深色页面白色箭头） |
| 保存 | 调用系统 `showAssetsCreationDialog`，用户可取消 |

---

## 三、项目结构

```
entry/src/main/ets/
├── entryability/EntryAbility.ets      # Ability 入口，沉浸式窗口初始化
├── entrybackupability/                # 备份扩展（模板）
├── model/
│   ├── AlbumStore.ets                 # 相册 CRUD + preferences 持久化
│   ├── FrameConfig.ets                # 相框配置 + 预设 + 自定义存储
│   └── ExifHelper.ets                 # EXIF 读取与格式化
├── pages/Index.ets                    # 主页面：Navigation + 三 Tab + 悬浮胶囊导航
├── service/FrameExporter.ets          # 相框渲染与导出（Canvas → JPEG → 相册）
├── util/WindowUtils.ets               # 状态栏颜色切换工具
└── view/
    ├── GalleryTab.ets                 # 相册页：网格、捏合缩放、导入、新建相册弹窗
    ├── DesignTab.ets                  # 设计页：预览、预设、滑块、开关、保存
    ├── PhotoEditPage.ets              # 相框编辑页：大图预览 + 底部相框选择条
    ├── FramePreview.ets               # 相框预览组件（设计页与编辑页共用）
    └── SettingsTab.ets                # 设置页：账号演示、购买演示、关于
```

---

## 四、目前进展

### ✅ 已完成

| 功能 | 状态 | 备注 |
|------|------|------|
| 沉浸式悬浮胶囊导航 | ✅ | 白色磨砂玻璃，62% 宽，选中蓝色/未选中黑色 |
| 相册管理 | ✅ | 新建相册、导入照片、空相册长按删除、向已有相册追加照片 |
| 照片网格浏览 | ✅ | 正方形卡片、双指捏合 1~5 列、圆角 14、未处理角标 |
| EXIF 读取 | ✅ | 光圈/快门/ISO/焦距/机型/时间/GPS 完整格式化 |
| 相框预设 | ✅ | 5 套内置（极简白、经典黑、杂志、胶片、暗夜） |
| 相框自定义设计 | ✅ | 背景色、文字颜色、边框厚度、文字大小、位置、对齐、开关项、自定义标题 |
| 实时预览 | ✅ | 设计页 240px 预览，编辑页全宽预览 |
| 相框编辑页 | ✅ | 大图 + 底部横向相框选择条（无边框 + 各预设实时缩略图） |
| 导出到相册 | ✅ | Canvas 渲染 → JPEG → 系统保存对话框 |
| 状态栏适配 | ✅ | 透明状态栏，安全区高度注入 AppStorage |
| 持久化 | ✅ | 相册、自定义相框、登录状态均存 preferences |

### 🔄 待完成 / 规划中

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 相框编辑页文字实时编辑 | 高 | 当前在设计页调参后进入编辑页，期望编辑页也能直接改参数 |
| 批量导出 | 中 | 相册内多选照片，统一应用相框并导出 |
| 分享功能 | 中 | 导出后直接唤起系统分享面板 |
| 真实账号体系 | 低 | 当前为演示登录，后续可接入华为账号 |
| 会员订阅 | 低 | 当前为演示按钮，后续接入 IAP |
| 照片原图保护 | 低 | 导出时保留原始 EXIF，或可选是否写入新相框信息 |
| 更多相框风格 | 低 | 胶片齿孔、拍立得、徕卡红标等 |

### 📐 设计稿

项目根目录 `pic/` 下存放了各阶段 UI 设计稿：

- `1.jpg` ~ `3.jpg`：早期概念稿（底部传统 TabBar、日期分组网格）
- `4.png`：相框编辑页概念（大图预览 + 底部相框条）
- `5.png`：相册页概念（日期分组、磨砂玻璃导航、圆形添加按钮）
- `6.jpg`：底部胶囊导航参考（iOS 风格，62% 宽白色胶囊，蓝色选中态）

---

## 五、技术栈

- **语言**：ArkTS（TypeScript 子集）
- **UI 框架**：ArkUI（声明式 UI）
- **API 版本**：HarmonyOS API 12+
- **关键 Kit**：
  - `@kit.ArkUI` — 界面与交互
  - `@kit.ArkGraphics2D` — Canvas 绘制与导出
  - `@kit.ImageKit` — 图片解码与 EXIF 读取
  - `@kit.MediaLibraryKit` — 照片选择与保存
  - `@kit.ArkData` — preferences 轻量持久化
  - `@kit.AbilityKit` — Ability 生命周期与上下文

---

## 六、如何继续

1. **查看设计稿**：`pic/` 目录下每张图对应一个设计阶段，按编号顺序阅读可看到 UI 演进。
2. **运行项目**：在 DevEco Studio 中打开，连接 HarmonyOS 真机或模拟器，点击运行。
3. **添加功能**：参考「待完成」列表，从编辑页文字实时编辑入手，改动面最小、体验提升最明显。
